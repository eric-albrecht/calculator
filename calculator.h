//prints out print message nd returns the number.
int readNumber(char* printMessage);

//prints out print message and returns the operator.
void readOperand(char* printMessage, char* operandStorage);

//runs entire calculator. Takes in two numbers, operand, then completes operation and prints it out.
void calculator();
